

const names = ['Nina', 'Iva', 'Juraj'];
console.log('names length is: ', names.length);

const firstItem = names[0];
console.log('first item is', firstItem);

for (let i = 0; i < names.length; i = i + 1) {
    const item = names[i];
    console.log(item);
}

