
const object = {};          // empty object

const gradesFromItClass = {
    'Nina': 1,
    'Juraj': 3,
    'Iva': 2,
    'nested_object': {
        'best_student': 'Nina'
    }
};

console.log(gradesFromItClass.Iva);
console.log(gradesFromItClass.nested_object.best_student);

const string = `I'm some string`;

console.log(string.toUpperCase());
